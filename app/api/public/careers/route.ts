import { NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
export const dynamic="force-dynamic";
export async function GET(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return NextResponse.json({positions:[]});const supabase=createClient(url,key,{auth:{persistSession:false}});const {data,error}=await supabase.from("career_positions").select("id,title,department,location,employment_type,summary,published_at").eq("status","published").order("published_at",{ascending:false});return NextResponse.json({positions:error?[]:data||[]});}
export async function POST(request:Request){try{const body=await request.json();const positionId=String(body.positionId||'');const name=String(body.name||'').trim();const email=String(body.email||'').trim();const message=String(body.message||'').trim();if(!positionId||!name||!email.includes('@')||message.length<20)return NextResponse.json({error:'Complete the required application details.'},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:'Career applications are not configured.'},{status:503});const admin=createClient(url,key,{auth:{persistSession:false}});const {data:position}=await admin.from('career_positions').select('id,status').eq('id',positionId).eq('status','published').maybeSingle();if(!position)return NextResponse.json({error:'This position is no longer accepting applications.'},{status:404});const {data,error}=await admin.from('career_applications').insert({position_id:positionId,name,email,location:String(body.location||''),portfolio_url:String(body.portfolioUrl||''),linkedin_url:String(body.linkedinUrl||''),message}).select('id').single();if(error)throw error;return NextResponse.json({success:true,id:data.id});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Application could not be sent.'},{status:500})}}
