import { NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
export async function POST(request:Request){try{const body=await request.json();const name=String(body?.name||"").trim(),email=String(body?.email||"").trim(),topic=String(body?.topic||"").trim(),message=String(body?.message||"").trim();if(!name||!email.includes("@")||!topic||message.length<10)return NextResponse.json({error:"Complete name, email, topic and message."},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:"Contact storage is not configured."},{status:503});const admin=createClient(url,key,{auth:{persistSession:false}});const {data,error}=await admin.from("contact_submissions").insert({user_id:body?.userId||null,name,email,topic,message,metadata:{user_agent:request.headers.get("user-agent")}}).select("id").single();if(error)throw error;return NextResponse.json({success:true,id:data.id});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Message could not be sent."},{status:500});}}
