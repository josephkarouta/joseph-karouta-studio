"use client";

import { Check, CircleDollarSign, Sparkles } from "lucide-react";
import SiteHeader from "@/components/site-header";
import SiteFooter from "@/components/site-footer";
import PricingAction from "@/components/account/PricingActions";
import { useAuth } from "@/components/auth-provider";
import { Eyebrow, GlassCard, PageContainer } from "@/components/ui/heyy";
import { normalizePlan, PLANS } from "@/lib/platform/plans";

export default function PricingPage() {
  const { plan } = useAuth();
  const current = normalizePlan(plan);
  return <main className="heyy-page"><SiteHeader/><section className="relative overflow-hidden pt-[var(--header-height)]"><div className="absolute inset-0 bg-[radial-gradient(circle_at_15%_5%,rgba(239,63,180,.13),transparent_30rem),radial-gradient(circle_at_85%_15%,rgba(46,124,246,.14),transparent_32rem)]"/><PageContainer className="relative py-16 sm:py-24"><Eyebrow>Plans & credits</Eyebrow><h1 className="mt-5 max-w-5xl text-5xl font-black leading-[.94] tracking-[-.065em] sm:text-7xl">One creative platform. Use only what you need.</h1><p className="mt-6 max-w-3xl text-base font-semibold leading-8 text-[var(--text-secondary)] sm:text-lg">The same reliable text intelligence is available across plans. Plans differ mainly by monthly credits, generation volume and support. Expert production is always quoted separately.</p></PageContainer></section><section className="border-t border-[var(--border)] bg-[var(--surface)] py-16"><PageContainer><div className="grid gap-4 lg:grid-cols-3">{PLANS.map((item)=><GlassCard key={item.id} className={`relative p-7 ${item.highlighted ? "border-[var(--accent-border)] ring-2 ring-[var(--accent-soft)]" : ""}`}>{item.highlighted && <span className="absolute right-5 top-5 rounded-full bg-[var(--accent)] px-3 py-1 text-[.62rem] font-black uppercase tracking-[.15em] text-white">Best starting point</span>}<p className="text-sm font-black text-[var(--accent-strong)]">{item.name}</p><p className="mt-4 text-5xl font-black tracking-[-.06em]">${item.monthlyPriceUsd}<span className="text-sm tracking-normal text-[var(--text-muted)]">/month</span></p><p className="mt-3 text-sm font-semibold leading-6 text-[var(--text-secondary)]">{item.description}</p><div className="mt-6 flex items-center gap-2 rounded-2xl bg-[var(--accent-soft)] px-4 py-3 text-sm font-black text-[var(--accent-strong)]"><Sparkles size={16}/>{item.monthlyCredits.toLocaleString("en-US")} credits each month</div><ul className="mt-6 space-y-3">{item.features.map(feature=><li key={feature} className="flex gap-3 text-sm font-semibold text-[var(--text-secondary)]"><Check size={16} className="mt-0.5 shrink-0 text-emerald-500"/>{feature}</li>)}</ul><PricingAction planId={item.id} current={current===item.id}/></GlassCard>)}</div><GlassCard className="mt-5 grid gap-6 p-7 md:grid-cols-[auto_1fr] md:items-center"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent-strong)]"><CircleDollarSign size={22}/></span><div><h2 className="text-xl font-black tracking-[-.035em]">Credit top-ups are supported by the wallet architecture.</h2><p className="mt-2 text-sm font-semibold leading-6 text-[var(--text-secondary)]">Pack sizes and final pricing remain centrally configurable while real provider costs are tested. Your monthly credits and purchased credits remain separate and visible.</p></div></GlassCard><p className="mt-6 text-center text-xs font-bold text-[var(--text-muted)]">Testing configuration shown in US dollars. Final launch allowances and prices can be changed from environment settings without redesigning the platform.</p></PageContainer></section><SiteFooter/></main>;
}
