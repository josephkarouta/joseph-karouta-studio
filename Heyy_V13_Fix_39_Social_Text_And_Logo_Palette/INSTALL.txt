Fix 39 — Social text safe areas and logo-derived Brand Guidelines palette

Replace/add only the included files.
No SQL is required.

After installation run:
  npx tsc --noEmit --pretty false
  npm run build

For existing Brand projects, open Guidelines. The palette is synced automatically from the selected/retained logo. Click Update Guideline System once to save a new guideline asset using the logo-derived palette.
Regenerate the Social Media System to create outputs with wrapped, safe-area text.
